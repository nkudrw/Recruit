<%= form_for(@point) do |f| %>
  <%= f.hidden_field :skill_id, :value => skill.id %>
  <%= f.hidden_field :user_id, :value => @user.id %>
  <% end %>
  <%= f.submit 'プラス', class: 'btn btn-primary btn-block' %>


<% end %>
